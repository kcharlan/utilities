#!/bin/bash
set -euo pipefail

WORKTREE_PATH="${1:-}"
STATUS="${2:-}"
TASK_ID="${3:-}"

if [ -z "$WORKTREE_PATH" ]; then
  exit 0
fi

if [ -d "$WORKTREE_PATH/.git" ] || git -C "$WORKTREE_PATH" rev-parse --git-dir >/dev/null 2>&1; then
  MAIN_REPO="$(git rev-parse --show-toplevel)"
  BRANCH_NAME="$(git -C "$WORKTREE_PATH" branch --show-current)"

  if [ "$STATUS" = "done" ] && [ -n "$BRANCH_NAME" ]; then
    git -C "$MAIN_REPO" merge --squash "$BRANCH_NAME" >/dev/null
    if [ -n "$TASK_ID" ]; then
      git -C "$MAIN_REPO" commit -m "feat(pipeline): plan $TASK_ID" >/dev/null
    else
      git -C "$MAIN_REPO" commit -m "feat(pipeline): merged worker branch" >/dev/null
    fi
  fi

  if [ "$STATUS" = "done" ] || [ "$STATUS" = "recovery" ]; then
    git -C "$MAIN_REPO" worktree remove --force "$WORKTREE_PATH" >/dev/null 2>&1 || true
    if [ -n "$BRANCH_NAME" ]; then
      git -C "$MAIN_REPO" branch -D "$BRANCH_NAME" >/dev/null 2>&1 || true
    fi
  fi
else
  if [ "$STATUS" = "done" ] || [ "$STATUS" = "recovery" ]; then
    rm -rf "$WORKTREE_PATH"
  fi
fi

exit 0
